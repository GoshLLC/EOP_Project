1. Install XAMPP (can be found in artifact folder in OneDrive, or downloaded from source). Make sure you install it in C:\xampp\ (default).
2. Launch XAMPP Control Panel, click "Start" on Apache and MySQL.
3. Move files from GitHub to C:\xampp\htdocs folder.
4. Visit http:/localhost/phpmyadmin on browser of choice.
5. Create a database (if it doesn’t exist) named "Eastern_Oregon_Pets". Import animals.sql to phpMyAdmin.
6. To launch website go to http://localhost on your browser of choice.

* note, main program is index.php, not index.html. "deleted" file has most of the bootstrap template.